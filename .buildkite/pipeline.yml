steps:
  - label: ":pipeline: Get Build Pipeline"
    command: buildkite-agent pipeline upload .buildkite/build.yml
  - wait
  - label: ":pipeline: Get release pipeline"
    command: buildkite-agent pipeline upload .buildkite/release.yml